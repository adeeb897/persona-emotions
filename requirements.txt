# Exact pins. Verified working together on this pod (H100 80GB, driver CUDA
# 13.0) -- do not float these, a patch bump can change hook or template
# behaviour mid-run.
#
# torch is NOT pinned here: it ships with the Runpod image as 2.4.1+cu124 and
# is CUDA-matched to it. Reinstalling it on every pod restart would cost a
# 2.5GB download for nothing.
#
# transformers is deliberately 4.x, NOT 5.x. transformers>=5 requires torch
# >=2.5 and silently disables PyTorch entirely under 2.4.1 -- AutoModelForCausalLM
# raises ImportError at import. Pinning 4.57.6 keeps the image's torch usable,
# so a fresh pod is `pip install -r requirements.txt` and nothing else.
# If you ever move to transformers 5.x, you must upgrade torch to >=2.5 first
# and re-verify extract.py's response-token slicing.
transformers==4.57.6
accelerate==1.14.0
huggingface_hub==1.27.0
pandas==3.0.5
pyarrow==25.0.1
numpy==2.4.6
scikit-learn==1.9.0
matplotlib==3.11.1
httpx==0.27.2
tqdm==4.70.0

# Notebooks. ipykernel is enough for VS Code Remote; jupyterlab only if you
# want to run a server and forward the port yourself.
ipykernel==6.29.5
jupyterlab==4.2.5
# pandas 3.x needs jinja2 >= 3.1.5 for DataFrame.style; the image ships 3.1.3,
# which pandas reports as "jinja2 not installed".
jinja2==3.1.6
